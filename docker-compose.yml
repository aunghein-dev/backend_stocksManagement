version: "3.8"

services:
  app:
    build:
      context: .
    container_name: backend-stock
    ports:
      - "127.0.0.1:9999:8080"
    restart: unless-stopped
    volumes:
      - /data/GeoLite2-City.mmdb:/data/GeoLite2-City.mmdb:ro  # mount host file into container (read-only)
